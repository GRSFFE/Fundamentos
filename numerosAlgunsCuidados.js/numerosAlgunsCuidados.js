console.log(7 / 0) // Qualquer número dividido por 0 em JS, gera o Infinity.

console.log("10" / 2) // Mesmo sendo string, quando tem um número o VSCode investiga oq tem dentro das Aspas.

console.log("3" + 2) // a string ganha.

console.log("Show" * 2) // Em outras linguagens o "show" sairia duas vezes, mas não no JS, que só multiplica se for número.

console.log(0.1 + 0.7) // Gera uma imprecisão por causa do ponto flutuante. 

console.log((10.345).toFixed(2))
